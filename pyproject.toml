[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "cni"
version = "0.1.0"
description = "CNI: rule-based Chinese interpreter (isa/of facts, no LLM)"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]

[project.scripts]
cni = "cni.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
cni = ["data/world/*.tm"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
